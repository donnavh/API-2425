<div class="card">
  <h2 class="h2">{{ item.name }}</h2>
  <img class="card__image" src="{{ item.image.src }}" style="view-transition-name:song_{{ item.id }}" alt="{{ item.image.alt }}" width="{{ item.image.width }}" height="{{ item.image.height }}" />
</div>
